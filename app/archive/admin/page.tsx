import type { Metadata } from "next";
import ArchiveAdminPage from "../../archive-admin-page";

export const metadata: Metadata = {
  title: "内容后台｜宇宙放映",
  robots: { index: false, follow: false },
};

export default ArchiveAdminPage;
